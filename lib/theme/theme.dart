import 'package:flutter/material.dart';

class CustomTheme {
  static ThemeData get lightTheme {
    return ThemeData(
      primaryColor: Color.fromRGBO(238, 236, 230, 1),
      scaffoldBackgroundColor: Colors.white,
    );
  }
}
